//Works as an object, at the end of the file this should be returned so that it can be used as an object in the jenkinsfile
def deployit(){
    echo "Deploying the application using the groovy Script..."
}

return this
